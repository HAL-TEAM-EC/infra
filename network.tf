# Network resources are intentionally not created yet.
#
# The current backend directory does not contain a Laravel application, so creating
# VPC, NAT Gateway, ALB, ECS, or RDS resources now would add cost without a runnable
# backend. Add network resources when the Laravel API deployment target is decided.
